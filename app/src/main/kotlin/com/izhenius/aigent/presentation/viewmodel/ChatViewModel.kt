package com.izhenius.aigent.presentation.viewmodel

import androidx.lifecycle.ViewModel

class ChatViewModel : ViewModel() {
    val title: String = "Hello Compose + Navigation + ViewModel + Material3"
}